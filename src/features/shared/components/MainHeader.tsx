export function MainHeader() {
    return (
        <header>
            Recipes App (link a /recipes)
            Mis recetas (link a /recipes/me)
            Crear receta (link a /recipes/new)
            Cerrar sesión (botón que borra el token de sesión y redirige a /login)
        </header>
    );
}